To nie jest błąd w kodzie Twojego programu ani w skrypcie .bat. Komunikat "System Windows nie może uzyskać dostępu do określonego urządzenia, ścieżki lub pliku" (widoczny na ostatnim zdjęciu) w środowisku korporacyjnym (domena mBank, co widać po ścieżkach) oznacza na 99%, że system bezpieczeństwa zablokował Twój nowy plik .exe.
Oto diagnoza i sposób rozwiązania:
Dlaczego to się dzieje?
 * CyberArk EPM (Endpoint Privilege Manager): Na zrzucie ekranu z właściwościami pliku widać zakładkę "CyberArk EPM". To oprogramowanie, które pilnuje, aby na komputerach banku nie uruchamiały się "nieznane" programy.
 * Zmiana sumy kontrolnej: Starsze wersje działają, bo CyberArk je już "poznał" lub zostały zatwierdzone. Nowa wersja (1.0.17), którą właśnie skompilowałeś, jest dla systemu zupełnie nowym, nieznanym plikiem. Ponieważ nie jest podpisana cyfrowym certyfikatem zaufanym przez bank, system ją blokuje (tzw. Application Control).
 * Fałszywy alarm antywirusowy: Pliki tworzone przez PyInstaller (którego używasz, co widać w logach) są bardzo często błędnie wykrywane jako wirusy przez systemy korporacyjne, ponieważ hakerzy często używają tego samego narzędzia do pakowania wirusów.
JAK TO NAPRAWIĆ? (Kroki od najprostszego)
KROK 1: Test lokalny (Omiń sieć)
Uruchamianie "nieznanego" pliku EXE bezpośrednio z zasobu sieciowego (\\mbank.pl\zasoby...) jest traktowane przez Windows jako najwyższe zagrożenie.
 * Skopiuj plik TeamFlowApp.exe (ten nowy, który nie działa) ręcznie na swój Pulpit.
 * Kliknij na niego prawym przyciskiem myszy -> Właściwości.
 * Na dole zakładki "Ogólne", jeśli widzisz pole Zabezpieczenia z polem wyboru Odblokuj (Unblock), zaznacz je i kliknij OK.
 * Spróbuj uruchomić plik z Pulpitu.
   * Jeśli zadziała: Problem leży tylko w uruchamianiu z sieci (skrypt .bat z "Metodą Hybrydową", który wysłałem wcześniej, powinien to rozwiązać, bo on kopiuje plik lokalnie przed uruchomieniem).
   * Jeśli nadal błąd: Blokuje Cię CyberArk/Antywirus na poziomie pliku.
KROK 2: Przekompiluj w "trybie czystym"
Czasami PyInstaller zostawia śmieci, które triggerują antywirusa. Zrób dokładnie tak:
 * Usuń całkowicie foldery build i dist w swoim projekcie.
 * Użyj tej komendy (zmieniłem ją, by była "lżejsza" dla antywirusa):
   pyinstaller --noconfirm --onefile --windowed --name "TeamFlowApp" --clean --noupx --version-file "version.txt" --add-data "config.ini;." --add-data "icons;icons" main.py

   (Kluczowe jest tu --noupx i --clean, które już stosowałeś, ale upewnijmy się, że foldery były puste przed startem).
KROK 3: Zmiana lokalizacji tymczasowej
Jeśli Twój skrypt .bat pobiera plik do %TEMP%, CyberArk może blokować uruchamianie plików wykonywalnych z tego konkretnego folderu (polityka bezpieczeństwa: "nie uruchamiaj nic z Temp").
Spróbuj zmienić w skrypcie .bat lokalizację docelową na folder, w którym masz prawo uruchamiać programy, np. C:\Users\TwojLogin\TeamFlowApp (utwórz taki folder ręcznie na dysku C i tam celuj skryptem).
W skrypcie .bat (wersja hybrydowa) zmień linię:
set "LOCAL_DIR=%TEMP%\TeamFlowApp"
na:
set "LOCAL_DIR=C:\Users\%USERNAME%\TeamFlowApp_Local"
Podsumowanie dla Ciebie
Błąd, który widzisz, nie wynika z błędu w kodzie Pythona. Twoja aplikacja jest poprawna. Po prostu systemy bezpieczeństwa w Twojej firmie zablokowały ten konkretny plik .exe, bo jest nowy.
Co zrobić teraz?
Skorzystaj z ostatniego skryptu (Metoda Hybrydowa), który Ci podałem. On wykonuje następujące czynności, które pomagają ominąć ten błąd:
 * Pobiera plik z sieci na dysk lokalny.
 * Używa komendy Unblock-File (w PowerShell), co "mówi" Windowsowi, że plik jest bezpieczny.
 * Dopiero wtedy go uruchamia.
Jeśli po użyciu tamtego skryptu nadal masz ten błąd – musisz skontaktować się z administratorem bezpieczeństwa (IT), aby dodał Twój folder lub sumę kontrolną pliku do wyjątków w CyberArk, ponieważ jako zwykły użytkownik nie obejdziesz blokady "Access Denied" nałożonej przez EPM.
