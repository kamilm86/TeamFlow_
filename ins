# schedule_model.py -> NOWA, POPRAWIONA WERSJA METODY

    def update_data(self, processed_data, year, month):

# staffing_details_window.py

    def _calculate_matrix_from_forecast(self, filtered_data, column_name):

# staffing_details_window.py

    def load_and_display_data(self):

# split_table_view.py

    def adjust_employees_table_width(self):

# main.py

    def _apply_dynamic_sizes(self):

# main.py -> wewnątrz klasy WorkScheduleWindow

    def showEvent(self, event):

# styles.py

class AppStyles: all

# schedule_model.py -> 
        elif hour >= 22 or hour <= 4:
            return QColor(30, 30, 30)

@echo off
echo Kopiowanie plikow aplikacji na dysk lokalny, prosze czekac...

:: Ustawia sciezke do folderu tymczasowego uzytkownika
set "LOCAL_APP_PATH=%TEMP%\TeamFlowApp"

:: Kopiuje caly folder aplikacji z dysku sieciowego na dysk lokalny (tylko jesli sa zmiany)
xcopy /s /e /d /y "%~dp0" "%LOCAL_APP_PATH%\"

echo Uruchamianie aplikacji...

:: Uruchamia aplikacje z lokalnej kopii
start "" "%LOCAL_APP_PATH%\TeamFlowApp.exe"

exit
